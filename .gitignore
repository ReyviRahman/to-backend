# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with Litmus
*.out

# Dependency directories (Go handles this via go.mod, but vendor can be ignored)
vendor/

# Environment variables (SANGAT PENTING: jangan sampai password Supabase bocor)
.env
.env.local
.env.*.local

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo

# OS metadata
.DS_Store
Thumbs.db

# Docker
.docker/

# Go build output
/bin/
dist/
/backend